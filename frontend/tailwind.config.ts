// Tailwind v4 uses CSS-first config — theme tokens (colors, fonts) live in
// src/styles/globals.css's `@theme` block, not here. This file is intentionally
// unused; kept only because it's part of the repo's documented file structure.
export default {};
